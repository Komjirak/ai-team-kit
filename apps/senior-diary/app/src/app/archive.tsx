import React from 'react';
import { StubScreen } from '@/components/StubScreen';

/**
 * P3 — 지난 이야기 (부모). 이번 슬라이스에서는 stub.
 * 후속: 날짜 내림차순 리스트(날짜·질문·상태: 정리된 글 / 🌙 밤사이 정리 중 / 원음만) — PRD §9-4 P3.
 */
export default function ArchiveScreen() {
  return <StubScreen title="지난 이야기" />;
}
