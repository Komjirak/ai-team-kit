# 작업 하네스 — <서비스명>

세션을 시작할 때 **가장 먼저** 읽는 문서다. 사람이 읽는 소개서가 아니라 작업
전에 대조하는 기준이다 — 코드를 고치기 전에 여기 규칙을 확인하고, 고친 뒤에는
"지금 상태"를 갱신한다. **문서가 코드보다 앞서 있으면 거짓말이다.**

이 문서는 세션 부트스트랩용이라 **짧게 유지한다.** 전체 구현 이력·화면별 설계
메모 같은 깊은 맥락은 담지 않는다 — 기능별 문서와 `git log`, 아래 "더 볼 것"을
따라간다. 낡을 값(최신 커밋 해시 등)은 박아두지 않는다.

## 지금 상태 — <한 줄 요약: 무엇이 라이브이고 무엇이 진행 중인가>

<!--
여기가 이 문서의 심장이다. 세션이 끝날 때마다 갱신한다. 쓰는 법:
- 맨 위에 가장 최신 상태 한 단락. 이모지 마커로 성격 표시(✅ 완료 · ⚠️ 주의 · 🔴 사고 · 🔒 대기)
- "다음 세션이 챙길 것"을 명시적으로 남긴다 — ① ② ③ 번호로
- 해결된 항목은 지우지 말고 "✅ 해결됨 — (날짜)"로 강등해 아래로 내린다.
  같은 실수의 재발 방지 기록이 된다.
-->

(초기 상태 — 아직 코드가 없다. 첫 사이클: PD가 PRD를 만든다.)

## 고치기 전에 확인할 규칙

<!--
프로젝트가 자라면서 실사고에서 뽑은 규칙을 여기 쌓는다. 형식은 3열 고정:
영역 | 규칙 | 어기면(무슨 일이 났었나). "어기면" 열이 비어 있는 규칙은
아직 규칙이 아니다 — 근거 없는 취향은 넣지 않는다.
-->

| 영역 | 규칙 | 어기면 |
|---|---|---|
| (예시) 기능 게이트 | 등급을 직접 비교하지 않고 기능 카탈로그의 키로 검사 | 요금제가 바뀔 때 호출부를 전부 찾아다녀야 한다 |

## 커밋 전 검증

⚠️ **검증은 종료코드로 판정한다 — 출력을 파이프로 자르면 안 된다.**
올바른 꼴: `npm run verify > log 2>&1; [ $? -eq 0 ] && ...` 또는 `set -o pipefail`.

- **항상**: `npm run verify` — <typecheck + 프로젝트 규칙표. 생기기 전엔 tsc라도>
- **번들·네이티브에 손댔으면**: <export/빌드 검증 명령>
- **서버·비용에 손댔으면**: <서버 검증 명령>

## 세션 시작 점검 (매번)

- `git log --oneline -20`부터 본다 — 여러 툴에서 나눠 작업한다. 내가 만들지 않은
  커밋이 이미 쌓여 있을 수 있다.
- 기준 줄기는 `main` 하나다. 새 작업은 `git fetch origin && git checkout -b <브랜치>
  origin/main`으로 시작한다.
- <저장소 주소·서브모듈·배포 트리거 등 이 프로젝트의 지형을 여기 적는다>

## 확정된 결정

PO 결정의 요약표. 전문은 `docs/DECISIONS.md`.

| 결정 | 내용 | 현재 상태 |
|---|---|---|
| | | |

## 더 볼 것

- `TEAM.md` — 조직 · 일하는 방식 · 팀 규칙
- `docs/DECISIONS.md` — PO 결정 로그
- `docs/PLAYBOOKS/` — 심사 대응 · OTA/빌드 · 결제 · 디버깅 원칙
- <브랜드·디자인 시스템·기능별 단일 기준 문서가 생기면 여기 등록>
