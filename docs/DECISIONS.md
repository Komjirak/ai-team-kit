# PO 결정 로그

PO가 내린 결정을 번호·날짜와 함께 기록한다. **결정은 여기에만 쌓고, HARNESS에는
요약표만 둔다.** 나중 결정이 이전 결정을 대체하면 지우지 말고 "→ #N으로 대체됨"을
남긴다 — 채록에서 "08-08 지시가 08-05 지시를 대체한다"처럼 이력이 남아야
왜 바뀌었는지 다음 세션이 안다.

형식:

```
## #1 (YYYY-MM-DD) 결정 제목
- 결정: 무엇을 하기로 했나 (한 문장)
- 이유: 왜 (대안과 비교했으면 그것도)
- 영향: 어느 코드/문서/절차가 바뀌나
- 상태: 반영 완료 / 진행 중 / 대기
```

---

## #1 (2026-08-14) 조직 개편 — BD·PM·BRAND 신설, PD 범위 축소

- **결정**: 역할을 6개에서 9개로 늘린다. **BD**(시장·사업성·BM), **PM**(PRD·범위·
  일정·품질), **BRAND**(브랜드 아이덴티티·컨셉 디자인)를 신설하고, **PD**는
  "앱·서비스·프로모션 디자인 + UX Writing"으로 좁힌다.
- **이유**: PO 지시. 기존 PD 한 명이 PRD·UX·디자인 시스템·라이팅을 다 지고 있었고,
  사업성 검토는 담당이 아예 없어 오케스트레이터가 임시로 떠맡고 있었다(렉시오
  `BUSINESS_CASE.md`가 그 사례).
- **파생 결정 (오케스트레이터가 경계를 정하고 문서화, PO 확인 필요)**:
  1. **PRD 소유권을 PD → PM으로 옮긴다.** 단, PRD는 **한 파일**로 유지하고
     PM(문제·범위·성공 기준) → PD(화면·상태·카피) **순차**로 쓴다. 두 문서로
     쪼개면 기능의 기준이 갈라져 서로 어긋난다.
  2. **PM에게 위임 권한을 주지 않는다.** 위임은 오케스트레이터만 한다 —
     관리자가 둘이면 같은 일이 두 번 돌거나 아무도 하지 않는다.
     PM은 제품의 시간축(마일스톤), ORCH는 세션의 시간축을 본다.
  3. **BRAND가 원본, PD가 파생.** 컬러·타이포·톤은 `BRAND.md`에서만 정하고 PD의
     디자인 시스템은 그것을 파생시킨다.
- **영향**: `TEAM.md`(조직도·RACI·기능 사이클·위임 규칙·에스컬레이션·기록 체계),
  `.claude/agents/{bd,pm,brand}.md` 신설, `.claude/agents/pd.md` 개정,
  `.claude/commands/kickoff.md`, `README.md`, `docs/HARNESS.md`.
  신설 문서 2종 등록: `docs/ROADMAP.md`(PM), `<제품>/docs/BRAND.md`(BRAND).
- **상태**: **PO 승인 완료 (2026-08-14) — 확정.** 파생 결정 3건(PRD 소유권 ·
  PM 위임권한 없음 · BRAND 우선)이 승인됐다. 이제 조직 경계는 확정 사실이며,
  바꾸려면 새 결정 번호로 대체한다 (이 항목을 고쳐 덮지 않는다).

## #2 (2026-08-14) 역할 하네스 개편 — Role Definition 도입 + 협업 관행 반영

- **결정**: PO 지시로 9개 역할 하네스를 개편한다. ① 각 하네스 맨 앞에
  **Role Definition 8줄**(한 줄 정의·핵심 질문·성공의 정의·주 산출물(DRI)·
  입력→출력·하지 않는 일·권한 경계·참조 모델)을 붙인다. ② 협업으로 이름난
  팀들의 관행 중 **공통 요소만** 골라 각 역할에 적용한다. ③ PO의 철학을
  `TEAM.md` §0으로 성문화한다. ④ 팀 소개(조직도·주요 특성)를 `README.md`에 싣는다.
- **이유**: PO 지시. 기존 하네스는 "책임·규칙·DoD"는 있었지만 **역할의 경계와
  성공 기준이 한눈에 안 보였다.** 위임 프롬프트를 쓸 때마다 경계를 매번 다시
  설명해야 했고, 그게 세션마다 반복되는 비용이었다.
- **파생 결정 (오케스트레이터가 정하고 문서화, PO 확인 필요)**:
  1. **주 산출물 = DRI 선언.** 각 역할은 파일 하나의 주인이다 (Apple DRI /
     Amazon single-threaded owner). 소유자가 둘인 파일은 만들지 않는다.
  2. **표준 보고 형식 5줄**을 전 역할 공통으로 강제한다 (`TEAM.md` §3).
     5번 "리스크 한 줄"은 형식이 아니라 자기검증 장치다.
  3. **위임 패키지 5종**을 오케스트레이터의 의무로 못 박는다 (`TEAM.md` §3).
  4. **구조는 베끼지 않는다.** 스쿼드·트라이브 같은 조직 구조, 회의체(베팅
     테이블 등), 분기 OKR, SLO 수치 운영은 1인 PO 팀에 과해서 채택하지 않고
     개념만 가져왔다. 채택/미채택 목록과 출처는 `TEAM.md` §7에 남겼다.
- **영향**: `.claude/agents/*.md` 9종 전부, `TEAM.md`(§0 철학 · §3 위임 패키지·
  표준 보고 형식 · §7 관행의 뿌리), `README.md` 전면 개편, `AGENTS.md`.
- **상태**: **PO 승인 완료 (2026-08-14) — 확정.** 파생 결정 4건 전부 승인됐다
  ("지금 방식 좋아"). 다음 세션부터 **위임 패키지 5종**과 **표준 보고 형식 5줄**은
  권고가 아니라 **기본값**이다. 지키지 않은 위임은 오케스트레이터의 실수로 센다.

## #3 (2026-08-25) senior-diary — 다음 단계(MVP 가능성 검증) 진행 + 슬로건 방향 승인

- **결정**: PO 지시 "다음 단계까지 진행해보자. MVP로서 가능성 있는지." —
  BD의 Go-if 판정안을 딛고 **MVP 정의·가능성 검증 단계로 진행**한다.
  카피 방향 **"더 늦기 전에 물어보세요 / 들어보세요"** (BUSINESS_CASE §4-3의
  상실 회피 소구)를 PO가 승인했다 ("슬로건 괜찮다").
- **이유**: 시장 조사(BUSINESS_CASE) 2회 갱신 결과를 PO가 검토. 경쟁 판정
  ("승부처는 지속 설계 — 데일리 습관 × 자녀 피드백 루프 × 1년 뒤 책")을 전제로
  MVP로 성립하는지를 다음 검증 대상으로 삼는다.
- **범위 주의 (아직 결정 아님)**: BM(유료 선물형)·1차 타깃("멀리 사는 딸")은
  **BD 권고를 작업 가정**으로 쓴다 — 가격 확정, G1 스모크 광고 집행(과금·대외 발행),
  제품명·BRAND 확정은 여전히 PO 승인 대기 (BUSINESS_CASE §8).
- **영향**: PM이 `apps/senior-diary/docs/PRD.md` 앞부분(문제·범위·성공 기준·MVP
  판정 기준) 착수. APP이 G4 기술 스파이크(시니어 STT·응답 경로 실현성) 착수.
- **상태**: 진행 중.

## #4 (2026-08-26) senior-diary — 질문 맞춤 B안 채택 + 책 샘플 화면 추가

- **결정**: PO 지시 "B안이 나을 것 같아". **질문 맞춤은 B안(룰 기반)** — ① 자녀
  신청 시 프로필 5~7문항(호칭·고향·결혼/사별/이혼 상태·형제/자녀 구성·주된 일)
  ② 프로필 기반 질문 뱅크 조건 분기(예: 고향=서울이면 상경 질문을 동네 질문으로
  치환, 이혼·사별이면 배우자 챕터 대체) ③ 부모 화면 전 질문에 "이 이야기는
  넘어갈게요" 스킵(반복 스킵 주제 자동 제외) ④ 자녀의 민감 주제 스위치.
  AI 개인화(꼬리 질문)는 계속 V1.x. 또한 **책 샘플 화면**(디지털북 미리보기 —
  표지·목차·본문·QR 원음)을 프로토타입에 추가한다 — "샘플로라도 책 화면이
  보이면 설득력을 가진다"(PO).
- **이유**: 고정 시퀀스의 맹점 — 프로필과 어긋난 질문(서울 토박이에게 상경 질문)이나
  가족사와 충돌하는 질문(이혼 가정에 배우자 질문)은 어색함을 넘어 이탈 트리거(R2).
  룰 분기는 AI 없이 해결되며 appetite 영향이 작다(+3~5일 추정).
- **영향**: PRD §3-1(I1 프로필·I2 분기·스킵)·§3-4 O7 조정·책 파이프라인 섹션
  추가(PM) → PRD §9·프로토타입 갱신(PD, 책 샘플 화면 포함).
- **상태**: 반영 완료 (2026-08-26).

## #5 (2026-08-26) senior-diary — 차별화 4요소 확정, 구체화 착수 (Go), BRAND 착수

- **결정**: PO 지시 원문 요지 — **"다음 차별화 포인트로 구체화 진행. 개인적인
  목적으로라도 만들었으면 해."**
  1. **데일리 습관** (안부 + 반복성)
  2. **자녀의 반응 & 응원**
  3. **차곡차곡 모이는 음성 정보 + 기록**
  4. **1년 뒤 책으로 완성**
  방향: **자녀가 선물하고 부모가 습관처럼 사용하는 서비스** (= BM 유료 선물형 확정,
  BUSINESS_CASE §8-1 결정됨). **최대한 감성 중심** ("더 늦기 전에 물어보세요").
  **하루 목표를 놓쳤을 때 처리는 추가 검토** (오픈 이슈로 등록, 확정 아님).
  산출물 지시: 경쟁 시장 분석·차별화 정리(BD) / PRD 반영(PM) / **Brand 문서
  신설 — 네이밍 포함**(BRAND, = D4 착수 승인. 이름 후보까지, 확정은 PO).
- **이유**: "개인 목적으로라도 만든다"는 PO 의사 표명 — 수익 규모(G3)가 착수의
  전제 조건에서 사실상 해제됐다(작아도 만든다). 검증 게이트(G1·G2)는 사업 확장
  판단용으로 유지된다.
- **미결로 남는 것**: 가격(D5) · G1 스모크 광고 집행(D2 — 대외 발행·과금) ·
  제품명 확정(후보까지만) · D7(책 성립 기준).
- **영향**: `BUSINESS_CASE.md`(BD — 차별화 4요소 중심 재정리), `PRD.md`(PM —
  제품 원칙·미응답 오픈 이슈), `BRAND.md` 신설(BRAND — 이름 후보·톤·감성 원형).
- **상태**: 반영 완료 (2026-08-28). 네이밍은 #6에서 확정.

## #6 (2026-08-28) senior-diary — 제품명 "하루담" 가칭 확정 (D9)

- **결정**: PO가 **"하루담"을 가칭으로 확정.** BRAND 3차 숏리스트 2안 중 선택.
  - 탈락: **오늘물어** — "너무 반말투다. 연령대(부모 60~75)·따뜻함 강조를 고려하면
    부적합." (명령형이 재촉으로 읽힘 — BRAND가 §1에 남긴 톤 긴장이 실제 근거가 됨)
  - 채택: **하루담**(ハルダム) — 따뜻함·한일 호환(일본어 春 다리) 우위. "하루 한
    이야기가 담긴다."
- **성격**: **가칭(working name).** 정식 확정 전 상표(KIPRIS·J-PlatPat)·도메인·
  양대 스토어 정밀 확인이 남아 있다(GROWTH). 그 전까지 문서·디자인은 하루담으로 진행.
- **이유**: 부모 세대가 매일 부르는 이름이라 존대·따뜻함이 식별력·즉시성보다 우선.
- **영향**: `BRAND.md`(하루담으로 이름 확정 표기 + 비주얼 컨셉 §4 확정),
  `apps/senior-diary/docs/DESIGN_CONCEPT.md` 신설(디자인 컨셉 + Google Stitch
  프롬프트 — PO가 Stitch로 컨셉 시안 제작 예정).
- **상태**: 진행 중.
